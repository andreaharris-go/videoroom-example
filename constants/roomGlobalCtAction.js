const SET_ROOM_ID = 'set-room-id'
const SET_PARTICIPANTS = 'set-participants'
const SET_PARTICIPANT_COUNT = 'set-participant-count'

const roomGlobalCtAction = {
  SET_PARTICIPANTS,
  SET_ROOM_ID,
  SET_PARTICIPANT_COUNT,
}

export default roomGlobalCtAction;
