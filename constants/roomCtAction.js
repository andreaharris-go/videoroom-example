const PUSH_CLIENTS = 'push-client'
const SET_MY_NAME = 'set-my-name'
const SET_SESSION_ATTACHED = 'set-session-attached'

const roomCtAction = {
  PUSH_CLIENTS,
  SET_MY_NAME,
  SET_SESSION_ATTACHED,
}

export default roomCtAction;
